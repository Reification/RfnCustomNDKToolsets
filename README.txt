These MSVS toolsets are integrations for MSVS android native build workflow for NDK versions not yet supported by Microsoft
They likely will not work for uses beyond what Reification is putting them to. If we want to share them they will need some finishing up work.

